# Основной веб-фреймворк
Flask==3.0.3

# Авторизация и сессии
Flask-Login==0.6.3

# Elasticsearch клиент (версия 8.x совместима с нашим ES)
elasticsearch==8.13.0

# Извлечение текста из документов
pdfplumber==0.10.4
python-docx==1.1.0
striprtf==0.0.28

# Кэширование результатов поиска через Redis
flask-caching==2.3.0
redis==5.0.8

# WSGI-сервер (для продакшена, если захочешь перейти с dev-сервера Flask)
gunicorn==22.0.0

# Werkzeug (обычно тянется автоматически, но фиксируем версию)
Werkzeug==3.0.3